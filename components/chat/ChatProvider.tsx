"use client";

/**
 * Re-exports from ChatContext.tsx — the canonical chat provider.
 * This file exists for backward compatibility; the app layout uses
 * `@/components/chat/ChatContext` directly.
 */
export { ChatProvider, useChat, PageContextCtx } from "@/components/chat/ChatContext";
